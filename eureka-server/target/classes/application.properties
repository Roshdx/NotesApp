server.port=8761

# Standalone Eureka server - do not register with itself
eureka.client.register-with-eureka=false
eureka.client.fetch-registry=false

# Expose actuator endpoints
management.endpoints.web.exposure.include=*
